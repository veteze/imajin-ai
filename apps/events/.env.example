# Events Service

# Database
DATABASE_URL=postgres://user:password@localhost:5432/imajin_dev

# Service URLs (internal)
AUTH_SERVICE_URL=http://localhost:3001
AUTH_URL=http://localhost:3001
PAY_SERVICE_URL=http://localhost:3004
PROFILE_SERVICE_URL=http://localhost:3005
PROFILE_URL=http://localhost:3005
CHAT_SERVICE_URL=http://localhost:3007
CONNECTIONS_SERVICE_URL=http://localhost:3003
# Registry service (node self/DID lookup, #2000)
REGISTRY_SERVICE_URL=http://localhost:3000

# Webhooks
WEBHOOK_SECRET=your-webhook-secret
# Optional: POST check-in events to this URL (e.g. a WLED bridge for physical presence lighting)
CHECKIN_WEBHOOK_URL=

# Platform
PLATFORM_DID=did:imajin:platform
PLATFORM_FEE_PERCENT=3

# Service-to-service auth (pay settle endpoint)
PAY_SERVICE_API_KEY=your-api-key-here

# Public URLs (client-side)
NEXT_PUBLIC_AUTH_URL=http://localhost:3001
NEXT_PUBLIC_CHAT_URL=http://localhost:3007
NEXT_PUBLIC_CONNECTIONS_URL=http://localhost:3003
NEXT_PUBLIC_DOMAIN=imajin.ai
NEXT_PUBLIC_DYKIL_URL=http://localhost:3101
NEXT_PUBLIC_EVENTS_URL=http://localhost:3006
NEXT_PUBLIC_LEARN_URL=http://localhost:3103
NEXT_PUBLIC_MEDIA_URL=http://localhost:3009
NEXT_PUBLIC_PAY_URL=http://localhost:3004
NEXT_PUBLIC_PROFILE_URL=http://localhost:3005
NEXT_PUBLIC_REGISTRY_URL=http://localhost:3002
# http://localhost: for local dev, https://dev- for deployed dev, https:// for prod
NEXT_PUBLIC_SERVICE_PREFIX=http://localhost:
NEXT_PUBLIC_WWW_URL=http://localhost:3000

# Notify
NOTIFY_SERVICE_URL=http://localhost:3008
NOTIFY_WEBHOOK_SECRET=dev-secret-change-me

# Runtime
PORT=
NODE_ENV=
PLATFORM_FEE=

# Internal API key(s) for kernel-internal service calls made via @imajin/auth
# (session act-as validation, X-Acting-For delegation resolution, and any
# local attestation-emission callers). ATTESTATION_INTERNAL_API_KEY is
# canonical for attestation forwarding (#2037) — AUTH_INTERNAL_API_KEY is a
# deprecated one-release fallback for it, and also separately guards
# verify-delegation. Set both to match the kernel's values.
ATTESTATION_INTERNAL_API_KEY=""
AUTH_INTERNAL_API_KEY=""
# Node identity DID (from relay.relay_config.imajin_did after bootstrap)
NODE_DID=
