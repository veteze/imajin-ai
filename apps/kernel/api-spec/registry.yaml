openapi: "3.1.0"
info:
  title: imajin registry
  version: "2.0.0"
  description: Node registration, heartbeat, build attestation, audience scopes, interests, preferences, and specs for the imajin.ai network. Runs as a kernel service at /{service}/api/... on a shared node domain.
servers:
  - url: https://{node}.imajin.ai/{service}
    description: production node
    variables:
      node:
        default: jin
      service:
        default: registry
  - url: http://localhost:3000/{service}
    description: local development

tags:
  - name: registry

components:
  securitySchemes:
    cookieAuth:
      type: apiKey
      in: cookie
      name: imajin_session
    bearerAuth:
      type: http
      scheme: bearer
    attestation:
      type: http
      scheme: bearer
      description: "Ed25519-signed NodeAttestation or NodeHeartbeat payload. The payload itself carries the signature field; no separate HTTP auth header is used — the node's registered public key is used to verify the signature embedded in the request body."
  schemas:
    Node:
      type: object
      properties:
        id: { type: string, description: "Node DID (did:imajin:...)" }
        hostname: { type: string }
        subdomain: { type: string, example: "jin.imajin.ai" }
        services: { type: array, items: { type: string } }
        capabilities: { type: array, items: { type: string } }
        status: { type: string, enum: [active, stale, unreachable, expired, revoked] }
        version: { type: string }
        lastHeartbeat: { type: string, format: date-time }
        registeredAt: { type: string, format: date-time }
        expiresAt: { type: string, format: date-time }
    ApprovedBuild:
      type: object
      properties:
        version: { type: string }
        buildHash: { type: string }
        architecture: { type: string }
        releaseDate: { type: string, format: date-time }
        deprecated: { type: boolean }
    NodeAttestation:
      type: object
      required: [nodeId, publicKey, buildHash, hostname, signature, timestamp]
      properties:
        nodeId: { type: string }
        publicKey: { type: string, description: "Ed25519 public key (base64)" }
        buildHash: { type: string }
        hostname: { type: string }
        version: { type: string }
        sourceCommit: { type: string }
        services: { type: array, items: { type: string } }
        capabilities: { type: array, items: { type: string } }
        timestamp: { type: number }
        signature: { type: string }
    NodeHeartbeat:
      type: object
      required: [nodeId, timestamp, buildHash, version, signature]
      properties:
        nodeId: { type: string }
        timestamp: { type: number }
        buildHash: { type: string }
        version: { type: string }
        health: { type: object }
        signature: { type: string }
    NodeSelf:
      type: object
      required: [did, nodeOperatorDid, nodeFeeBps, buyerCreditBps]
      properties:
        did: { type: string, description: "This node's own DID (did:imajin:...)", example: "did:imajin:jin" }
        nodeOperatorDid: { type: [string, "null"], description: "DID of the human/entity operating this node, if configured" }
        nodeFeeBps: { type: [integer, "null"], description: "Node fee, in basis points, applied to .fair settlements on this node", example: 50 }
        buyerCreditBps: { type: [integer, "null"], description: "Buyer credit, in basis points, applied to .fair settlements on this node", example: 25 }
    Error:
      type: object
      required: [error]
      properties:
        error: { type: string }

paths:
  /api/health:
    get:
      tags: [registry]
      operationId: healthCheck
      summary: Service health check
      responses:
        "200":
          description: Service is healthy
          content:
            application/json:
              schema:
                type: object
                properties:
                  status: { type: string }
                  service: { type: string }
                  timestamp: { type: string, format: date-time }

  /api/spec:
    get:
      tags: [registry]
      operationId: getSpec
      summary: Serve OpenAPI spec (YAML)
      responses:
        "200":
          description: OpenAPI YAML spec
          content:
            text/yaml:
              schema: { type: string }

  /api/specs:
    get:
      tags: [registry]
      operationId: listServices
      summary: List all registered imajin services with their spec URLs
      responses:
        "200":
          description: Service list
          content:
            application/json:
              schema:
                type: object
                properties:
                  services:
                    type: array
                    items:
                      type: object
                      properties:
                        name: { type: string }
                        description: { type: string }
                        icon: { type: string }
                        label: { type: string }
                        visibility: { type: string }
                        category: { type: string }
                        url: { type: string }
                        spec: { type: string }

  /api/specs/{service}:
    get:
      tags: [registry]
      operationId: getServiceSpec
      summary: Proxy and return the OpenAPI spec for a named service
      parameters:
        - name: service
          in: path
          required: true
          schema: { type: string }
          description: "Service name (e.g. profile, events, auth)"
      responses:
        "200":
          description: Service spec (YAML or JSON as returned by service)
          content:
            text/yaml:
              schema: { type: string }
        "404":
          description: Unknown service
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }
        "502":
          description: Could not reach service
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }

  /api/builds/verify:
    get:
      tags: [registry]
      operationId: listApprovedBuilds
      summary: List all approved builds
      parameters:
        - name: version
          in: query
          schema: { type: string }
        - name: deprecated
          in: query
          schema: { type: boolean }
          description: "Include deprecated builds (default: false)"
      responses:
        "200":
          description: Approved builds list
          content:
            application/json:
              schema:
                type: object
                properties:
                  builds:
                    type: array
                    items: { $ref: "#/components/schemas/ApprovedBuild" }
    post:
      tags: [registry]
      operationId: verifyBuild
      summary: Check if a build hash is approved
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [buildHash]
              properties:
                buildHash: { type: string }
                version: { type: string }
      responses:
        "200":
          description: Verification result
          content:
            application/json:
              schema:
                type: object
                properties:
                  valid: { type: boolean }
                  source: { type: string, enum: [official, fork, unknown] }
                  version: { type: string }
                  architecture: { type: string }
                  releaseDate: { type: string, format: date-time }
                  deprecated: { type: boolean }
                  error: { type: string }
        "400":
          description: Missing buildHash
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }

  /api/node/self:
    get:
      tags: [registry]
      operationId: getNodeSelf
      summary: >-
        Public lookup for this node's own DID and minimal public config.
        Single source of truth for the node DID lookup previously
        re-implemented via raw `relay.relay_config` SQL in coffee, learn,
        events, and market (#2000).
      responses:
        "200":
          description: Node self metadata
          headers:
            Cache-Control:
              schema: { type: string, example: "public, max-age=30" }
          content:
            application/json:
              schema: { $ref: "#/components/schemas/NodeSelf" }
              example:
                did: "did:imajin:jin"
                nodeOperatorDid: "did:imajin:operator"
                nodeFeeBps: 50
                buyerCreditBps: 25
        "503":
          description: Node identity not configured yet (relay_config singleton row missing and no RELAY_DID fallback)
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }

  /api/node/register:
    post:
      tags: [registry]
      operationId: registerNode
      summary: Register a new node — verifies Ed25519 attestation and provisions subdomain
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [attestation]
              properties:
                attestation: { $ref: "#/components/schemas/NodeAttestation" }
                chainLog:
                  type: array
                  items: { type: string }
                  description: "Optional chain verification log"
      responses:
        "201":
          description: Node registered
          content:
            application/json:
              schema:
                type: object
                properties:
                  status: { type: string, enum: [verified] }
                  subdomain: { type: string, example: "jin.imajin.ai" }
                  expiresAt: { type: number }
                  chainVerified: { type: boolean }
        "200":
          description: Node renewed (existing node)
          content:
            application/json:
              schema:
                type: object
                properties:
                  status: { type: string, enum: [verified] }
                  subdomain: { type: string }
                  expiresAt: { type: number }
                  renewed: { type: boolean }
                  chainVerified: { type: boolean }
        "400":
          description: Invalid attestation structure or hostname format
          content:
            application/json:
              schema:
                type: object
                properties:
                  status: { type: string, enum: [rejected] }
                  error: { type: string }
                  hint: { type: string }
        "401":
          description: Invalid signature
          content:
            application/json:
              schema:
                type: object
                properties:
                  status: { type: string, enum: [rejected] }
                  error: { type: string }
        "403":
          description: Unknown or deprecated build hash
          content:
            application/json:
              schema:
                type: object
                properties:
                  status: { type: string, enum: [rejected] }
                  error: { type: string }
                  hint: { type: string }
        "409":
          description: Hostname taken or in grace period
          content:
            application/json:
              schema:
                type: object
                properties:
                  status: { type: string, enum: [rejected] }
                  error: { type: string }

  /api/node/heartbeat:
    post:
      tags: [registry]
      operationId: nodeHeartbeat
      summary: Node liveness ping — verifies Ed25519 signature against registered public key
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/NodeHeartbeat" }
      responses:
        "200":
          description: Heartbeat acknowledged
          content:
            application/json:
              schema:
                type: object
                properties:
                  ack: { type: boolean }
                  nextHeartbeat: { type: number, description: "Unix ms timestamp for next expected heartbeat" }
                  actions:
                    type: array
                    items:
                      type: object
                      properties:
                        type: { type: string, enum: [renew, reattest, upgrade] }
                        reason: { type: string }
                        expiresIn: { type: number }
                        targetVersion: { type: string }
        "400":
          description: Missing required fields
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }
        "401":
          description: Invalid signature
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }
        "403":
          description: Node expired or revoked
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }
        "404":
          description: Node not found
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }

  /api/node/list:
    get:
      tags: [registry]
      operationId: listNodes
      summary: List registered nodes
      parameters:
        - name: status
          in: query
          schema: { type: string, enum: [active, stale, all], default: active }
        - name: service
          in: query
          schema: { type: string }
          description: "Filter by service capability"
        - name: limit
          in: query
          schema: { type: integer, default: 100, maximum: 500 }
        - name: offset
          in: query
          schema: { type: integer, default: 0 }
      responses:
        "200":
          description: Node list
          content:
            application/json:
              schema:
                type: object
                properties:
                  nodes:
                    type: array
                    items: { $ref: "#/components/schemas/Node" }
                  total: { type: integer }
                  limit: { type: integer }
                  offset: { type: integer }
    head:
      tags: [registry]
      operationId: nodeStats
      summary: Network statistics in response headers
      responses:
        "200":
          description: Stats in headers
          headers:
            X-Total-Nodes:
              schema: { type: integer }
            X-Active-Nodes:
              schema: { type: integer }
            X-Stale-Nodes:
              schema: { type: integer }
            X-Unreachable-Nodes:
              schema: { type: integer }

  /api/node/lookup/{id}:
    get:
      tags: [registry]
      operationId: lookupNode
      summary: Look up a node by DID or hostname
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
          description: "Node DID (did:imajin:...), hostname (jin), or full subdomain (jin.imajin.ai)"
      responses:
        "200":
          description: Node found
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Node" }
        "404":
          description: Node not found
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }

  /api/node/resolve/{did}:
    get:
      tags: [registry]
      operationId: resolveNodeByDid
      summary: Resolve any DID (node DID or chain DID) to a node endpoint
      parameters:
        - name: did
          in: path
          required: true
          schema: { type: string }
          description: "did:imajin:... (node DID) or did:dfos:... (chain DID)"
      responses:
        "200":
          description: Node resolved
          content:
            application/json:
              schema:
                type: object
                properties:
                  node:
                    type: object
                    properties:
                      hostname: { type: string }
                      subdomain: { type: string }
                      status: { type: string }
                      services: { type: array, items: { type: string } }
        "400":
          description: DID is required
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }
        "404":
          description: No node found for this DID
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }

  /relay/{path}:
    get:
      tags: [registry]
      operationId: relayGet
      summary: DFOS web relay — catch-all GET handler
      description: |
        Reads are open — no authentication required, so the proof log stays
        publicly verifiable.
      parameters:
        - name: path
          in: path
          required: true
          schema: { type: string }
          description: "Relay sub-path (catch-all)"
      responses:
        "200":
          description: Relay response
    post:
      tags: [registry]
      operationId: relayPost
      summary: DFOS web relay — catch-all POST handler
      description: |
        Writes require a verified Imajin DID (#454). The effective DID is
        forwarded to the relay as `x-imajin-relay-writer` for audit; any
        client-supplied value for that header is discarded.
      security:
        - cookieAuth: []
        - bearerAuth: []
      parameters:
        - name: path
          in: path
          required: true
          schema: { type: string }
          description: "Relay sub-path (catch-all)"
      requestBody:
        required: false
        content:
          application/json:
            schema: { type: object }
      responses:
        "200":
          description: Relay response
        "401":
          description: Not authenticated — relay writes require a verified DID
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }
        "403":
          description: Not authorized to act as the delegated identity
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }
        "503":
          description: Auth service unavailable
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }
    put:
      tags: [registry]
      operationId: relayPut
      summary: DFOS web relay — catch-all PUT handler
      description: |
        Writes require a verified Imajin DID (#454). The effective DID is
        forwarded to the relay as `x-imajin-relay-writer` for audit; any
        client-supplied value for that header is discarded.
      security:
        - cookieAuth: []
        - bearerAuth: []
      parameters:
        - name: path
          in: path
          required: true
          schema: { type: string }
          description: "Relay sub-path (catch-all)"
      requestBody:
        required: false
        content:
          application/json:
            schema: { type: object }
      responses:
        "200":
          description: Relay response
        "401":
          description: Not authenticated — relay writes require a verified DID
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }
        "403":
          description: Not authorized to act as the delegated identity
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }
        "503":
          description: Auth service unavailable
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }

  /api/audience/{scope}:
    get:
      tags: [registry]
      operationId: getAudience
      summary: Get nodes/identities registered for an audience scope
      parameters:
        - name: scope
          in: path
          required: true
          schema: { type: string }
          description: Audience scope identifier
      responses:
        "200":
          description: Audience members for the scope
          content:
            application/json:
              schema:
                type: object
                properties:
                  scope: { type: string }
                  members: { type: array, items: { type: string } }
        "404":
          description: Scope not found

  /api/interests:
    get:
      tags: [registry]
      operationId: listInterests
      summary: List all registered interest scopes
      responses:
        "200":
          description: Interest scopes
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
                  properties:
                    scope: { type: string }
                    description: { type: string }
    post:
      tags: [registry]
      operationId: registerInterest
      summary: Register interest in a scope
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [scope]
              properties:
                scope: { type: string }
                did: { type: string }
      responses:
        "200":
          description: Interest registered

  /api/interests/{scope}:
    get:
      tags: [registry]
      operationId: getInterest
      summary: Get details for a specific interest scope
      parameters:
        - name: scope
          in: path
          required: true
          schema: { type: string }
      responses:
        "200":
          description: Interest scope details
        "404":
          description: Not found
    delete:
      tags: [registry]
      operationId: removeInterest
      summary: Deregister interest in a scope
      security:
        - bearerAuth: []
      parameters:
        - name: scope
          in: path
          required: true
          schema: { type: string }
      responses:
        "200":
          description: Interest removed

  /api/preferences/{did}:
    get:
      tags: [registry]
      operationId: getPreferences
      summary: Get all preference settings for a DID
      security:
        - cookieAuth: []
        - bearerAuth: []
      parameters:
        - name: did
          in: path
          required: true
          schema: { type: string }
      responses:
        "200":
          description: Preferences object
        "404":
          description: Not found
    put:
      tags: [registry]
      operationId: updatePreferences
      summary: Update preference settings for a DID
      security:
        - cookieAuth: []
        - bearerAuth: []
      parameters:
        - name: did
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object }
      responses:
        "200":
          description: Preferences updated

  /api/preferences/{did}/interests/{scope}:
    get:
      tags: [registry]
      operationId: getPreferenceInterest
      summary: Get interest preference for a specific scope and DID
      security:
        - cookieAuth: []
        - bearerAuth: []
      parameters:
        - name: did
          in: path
          required: true
          schema: { type: string }
        - name: scope
          in: path
          required: true
          schema: { type: string }
      responses:
        "200":
          description: Interest preference
        "404":
          description: Not found
    put:
      tags: [registry]
      operationId: setPreferenceInterest
      summary: Set interest preference for a scope
      security:
        - cookieAuth: []
        - bearerAuth: []
      parameters:
        - name: did
          in: path
          required: true
          schema: { type: string }
        - name: scope
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                enabled: { type: boolean }
                settings: { type: object }
      responses:
        "200":
          description: Preference updated
    delete:
      tags: [registry]
      operationId: deletePreferenceInterest
      summary: Remove interest preference for a scope
      security:
        - cookieAuth: []
        - bearerAuth: []
      parameters:
        - name: did
          in: path
          required: true
          schema: { type: string }
        - name: scope
          in: path
          required: true
          schema: { type: string }
      responses:
        "200":
          description: Preference removed
